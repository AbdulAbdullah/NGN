 # App
 APP_ENV=development
 APP_HOST=0.0.0.0
 APP_PORT=8080
 
 # Database
 DATABASE_URL=postgres://postgres:postgres@localhost:5432/ngn_verify
 
 # Auth
 JWT_SECRET=change-me-in-production
 JWT_ISSUER=ngn-verify
 JWT_AUDIENCE=ngn-verify-api
 JWT_ACCESS_TTL_SECONDS=3600
